services:
  - type: web
    name: w3stream-voice-agent
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python agent.py download-files && python agent.py dev
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.1
